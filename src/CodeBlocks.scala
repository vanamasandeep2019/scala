/*Code Block is multiple lines of code 
that are enclosed between braces i.e. everything 
between { and } is in one block code.*/


object CodeBlocks extends App{
  
  val block={
    val x=10;
    val y=10;
    if(x==y) true else false
    
  }
  println(block)
}